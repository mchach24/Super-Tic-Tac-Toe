// TODO: SEE TODO.md

/*define(['jquery', 'showdown'], function($, showdown) {

    var converter = new showdown.Converter();
    
    $.get('RULES.md', function(markdown) {
        processMD(markdown)
    }, 'text');

    function processMD(text) {
        html = converter.makeHtml(text);
        $('#rules-body').html(html);
    }

});*/